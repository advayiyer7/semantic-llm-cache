global:
  scrape_interval: 5s

scrape_configs:
  - job_name: semantic-cache
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
    # NOTE: /metrics is added in Phase 4. Until then this target reads as DOWN,
    # which is expected.
